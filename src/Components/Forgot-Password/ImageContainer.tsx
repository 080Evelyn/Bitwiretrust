import { verify_email_png } from "@/assets";

const ImageContainer = () => {
  return (
    <img
      src={verify_email_png}
      alt="img"
      className="w-full max-w-[95%] h-[95vh] mx-auto"
    />
  );
};

export default ImageContainer;
